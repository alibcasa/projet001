'use client'
import{useEffect,useState}from'react'
export function FlashcardsClient(){const[cards,setCards]=useState<any[]>([]);const[front,setFront]=useState('');const[back,setBack]=useState('');const[flip,setFlip]=useState<string|null>(null);async function load(){const r=await fetch('/api/flashcards');if(r.ok)setCards(await r.json())}useEffect(()=>{load()},[]);async function add(){if(!front||!back)return;await fetch('/api/flashcards',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({front,back})});setFront('');setBack('');load()}return <div className="space-y-5"><div className="grid gap-3 rounded-2xl border bg-white p-5 md:grid-cols-[1fr_1fr_auto]"><input className="rounded-xl border p-3" placeholder="Question / recto" value={front} onChange={e=>setFront(e.target.value)}/><input className="rounded-xl border p-3" placeholder="Réponse / verso" value={back} onChange={e=>setBack(e.target.value)}/><button className="rounded-xl bg-black px-5 text-white" onClick={add}>Ajouter</button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{cards.map(c=><button key={c.id} onClick={()=>setFlip(flip===c.id?null:c.id)} className="min-h-40 rounded-2xl border bg-white p-5 text-left"><p className="text-xs uppercase text-zinc-400">{flip===c.id?'Réponse':'Question'}</p><p className="mt-4 font-medium">{flip===c.id?c.back:c.front}</p></button>)}</div></div>}
