'use client'
import {useEffect,useState} from 'react'
export function ReferencesClient(){const[items,setItems]=useState<any[]>([]);const[title,setTitle]=useState('');const[author,setAuthor]=useState('');const[year,setYear]=useState('');async function load(){const r=await fetch('/api/references');if(r.ok)setItems(await r.json())}useEffect(()=>{load()},[]);async function add(){if(!title.trim())return;await fetch('/api/references',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({title,authors:author,year:year?Number(year):null})});setTitle('');setAuthor('');setYear('');load()}async function del(id:string){await fetch(`/api/references/${id}`,{method:'DELETE'});load()}return <div className="space-y-5"><div className="grid gap-3 rounded-2xl border bg-white p-5 md:grid-cols-4"><input className="rounded-xl border p-3" placeholder="Titre" value={title} onChange={e=>setTitle(e.target.value)}/><input className="rounded-xl border p-3" placeholder="Auteur(s)" value={author} onChange={e=>setAuthor(e.target.value)}/><input className="rounded-xl border p-3" placeholder="Année" value={year} onChange={e=>setYear(e.target.value)}/><button onClick={add} className="rounded-xl bg-black p-3 text-white">Ajouter</button></div><div className="space-y-3">{items.map(i=><div key={i.id} className="flex items-center justify-between rounded-xl border bg-white p-4"><div><p className="font-medium">{i.title}</p><p className="text-sm text-zinc-500">{i.authors||i.author||'Auteur inconnu'} {i.year?`· ${i.year}`:''}</p></div><button onClick={()=>del(i.id)} className="text-sm text-red-600">Supprimer</button></div>)}</div></div>}
