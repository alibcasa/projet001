'use client'
import{useEffect,useState}from'react'
export function UsersClient(){const[users,setUsers]=useState<any[]>([]);const[error,setError]=useState('');useEffect(()=>{fetch('/api/admin/users').then(async r=>{const d=await r.json();if(r.ok)setUsers(d);else setError(d.error)})},[]);return <div className="rounded-2xl border bg-white p-5">{error?<p className="text-red-600">{error}. Attribuez le rôle admin/super_admin dans profiles pour accéder à cette zone.</p>:<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-3">Nom</th><th className="p-3">ID</th><th className="p-3">Rôle</th><th className="p-3">Créé</th></tr></thead><tbody>{users.map(u=><tr key={u.id} className="border-b"><td className="p-3">{u.full_name||'—'}</td><td className="p-3 font-mono text-xs">{u.id}</td><td className="p-3">{u.role||'user'}</td><td className="p-3">{u.created_at?new Date(u.created_at).toLocaleDateString('fr-FR'):'—'}</td></tr>)}</tbody></table></div>}</div>}
