export function appOrigin(request:Request){return process.env.NEXT_PUBLIC_APP_URL||new URL(request.url).origin}
export function googleAuthUrl(request:Request){const redirect=`${appOrigin(request)}/api/integrations/google/callback`;const p=new URLSearchParams({client_id:process.env.GOOGLE_CLIENT_ID||'',redirect_uri:redirect,response_type:'code',access_type:'offline',prompt:'consent',scope:'https://www.googleapis.com/auth/drive.file https://www.googleapis.com/auth/documents https://www.googleapis.com/auth/spreadsheets https://www.googleapis.com/auth/calendar'});return `https://accounts.google.com/o/oauth2/v2/auth?${p}`}
export function microsoftAuthUrl(request:Request){const tenant=process.env.MICROSOFT_TENANT_ID||'common';const redirect=`${appOrigin(request)}/api/integrations/microsoft/callback`;const p=new URLSearchParams({client_id:process.env.MICROSOFT_CLIENT_ID||'',redirect_uri:redirect,response_type:'code',response_mode:'query',scope:'offline_access User.Read Files.ReadWrite.All Sites.ReadWrite.All'});return `https://login.microsoftonline.com/${tenant}/oauth2/v2.0/authorize?${p}`}
