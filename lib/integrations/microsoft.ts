export const microsoftScopes=["offline_access","User.Read","Files.ReadWrite.All","Sites.ReadWrite.All"]; export function microsoftAuthUrl(){const tenant=process.env.MICROSOFT_TENANT_ID||"common";const params=new URLSearchParams({client_id:process.env.MICROSOFT_CLIENT_ID||"",response_type:"code",redirect_uri:process.env.MICROSOFT_REDIRECT_URI||"",response_mode:"query",scope:microsoftScopes.join(" ")});return `https://login.microsoftonline.com/${tenant}/oauth2/v2.0/authorize?${params}`}
