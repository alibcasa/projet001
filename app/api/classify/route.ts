import { NextResponse } from 'next/server'; import { classifyText } from '@/lib/classification'; export async function POST(req:Request){const b=await req.json();return NextResponse.json(classifyText(`${b.title||''}\n${b.text||''}`))}
