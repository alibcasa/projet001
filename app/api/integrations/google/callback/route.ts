import{NextResponse}from'next/server';import{createClient}from'@/lib/supabase/server';import{appOrigin}from'@/lib/integrations/oauth'
export async function GET(r:Request){const url=new URL(r.url);const code=url.searchParams.get('code');if(!code)return NextResponse.redirect(`${appOrigin(r)}/integrations?error=google`);const redirect=`${appOrigin(r)}/api/integrations/google/callback`;const p=new URLSearchParams({code,client_id:process.env.GOOGLE_CLIENT_ID||'',client_secret:process.env.GOOGLE_CLIENT_SECRET||'',redirect_uri:redirect,grant_type:'authorization_code'});const token=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body:p});const data=await token.json();const s=await createClient();const{data:{user}}=await s.auth.getUser();if(user&&token.ok)await s.from('integrations').upsert({user_id:user.id,provider:'google',config:data,enabled:true},{onConflict:'user_id,provider'});return NextResponse.redirect(`${appOrigin(r)}/integrations`)}
