import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'

export default async function Dashboard(){const s=await createClient();const [{count:docs},{count:keynotes},{count:quizzes},{data:progress},{data:recent}]=await Promise.all([s.from('documents').select('*',{count:'exact',head:true}),s.from('keynotes').select('*',{count:'exact',head:true}),s.from('quizzes').select('*',{count:'exact',head:true}),s.from('reading_progress').select('completion_percent,reading_seconds'),s.from('documents').select('id,title,total_pages,created_at').order('created_at',{ascending:false}).limit(5)]);const seconds=(progress||[]).reduce((a:any,b:any)=>a+(b.reading_seconds||0),0);const avg=progress?.length?Math.round(progress.reduce((a:any,b:any)=>a+(b.completion_percent||0),0)/progress.length):0;return <div><div className="flex items-end justify-between"><div><h1 className="text-3xl font-bold">Dashboard</h1><p className="mt-2 text-zinc-500">Vue d'ensemble de votre apprentissage.</p></div><Link href="/library" className="rounded-xl bg-black px-4 py-2 text-sm text-white">Importer un PDF</Link></div><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['PDF',docs||0],['Keynotes',keynotes||0],['QCM',quizzes||0],['Temps de lecture',`${Math.round(seconds/3600)} h`]].map(([l,v])=><div key={String(l)} className="rounded-2xl border bg-white p-5"><p className="text-sm text-zinc-500">{l}</p><p className="mt-2 text-3xl font-bold">{v}</p></div>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_320px]"><section className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Documents récents</h2><div className="mt-4 space-y-2">{(recent||[]).map((d:any)=><Link key={d.id} href={`/reader/${d.id}`} className="block rounded-xl border p-3 hover:bg-zinc-50"><p className="font-medium">{d.title}</p><p className="text-xs text-zinc-500">{d.total_pages||0} pages</p></Link>)}</div></section><section className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Progression</h2><p className="mt-4 text-5xl font-bold">{avg}%</p><div className="mt-4 h-3 rounded-full bg-zinc-100"><div className="h-3 rounded-full bg-black" style={{width:`${avg}%`}}/></div><div className="mt-5 grid gap-2"><Link href="/revision" className="rounded-xl border p-3 text-sm">Centre de révision</Link><Link href="/agenda" className="rounded-xl border p-3 text-sm">Agenda</Link><Link href="/qcm" className="rounded-xl border p-3 text-sm">QCM</Link></div></section></div></div>}
